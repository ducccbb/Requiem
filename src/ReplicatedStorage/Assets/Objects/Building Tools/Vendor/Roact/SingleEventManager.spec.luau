-- Saved by UniversalSynSaveInstance (Join to Copy Games) https://discord.gg/wx4ThpAsmw

--[[
__________                   __               __    __________              .__
\______   \_______  ____    |__| ____   _____/  |_  \______   \ ____ _____  |  |
 |     ___/\_  __ \/  _ \   |  |/ __ \_/ ___\   __\  |       _// __ \\__  \ |  |
 |    |     |  | \(  <_> )  |  \  ___/\  \___|  |    |    |   \  ___/ / __ \|  |__
 |____|     |__|   \____/\__|  |\___  >\___  >__|    |____|_  /\___  >____  /____/
                        \______|    \/     \/               \/     \/     \/
]]
--                           Project Real  |  Luau Decompiler
--                                   Made by @zinvera
--File: ReplicatedStorage.Assets.Objects.Building Tools.Vendor.Roact.SingleEventManager.spec
--                               Dumped in 0.00146 seconds
--                          Bytecode version 9  |  25 functions

local u0 = script
script = nil
local v1 = getfenv(0)
v1.script = nil
_G.script = nil
v1 = getfenv(0)
v1.getfenv = nil
return function() -- Line: 8 -- upvalues: u0 (val)
    local assertDeepEqual = require(u0.Parent.assertDeepEqual)
    local createSpy = require(u0.Parent.createSpy)
    local Logging = require(u0.Parent.Logging)
    local SingleEventManager = require(u0.Parent.SingleEventManager)
    describe("new", function() -- Line: 15 -- upvalues: SingleEventManager (val)
        it("should create a SingleEventManager", function() -- Line: 16 -- upvalues: SingleEventManager (upval)
            expect((SingleEventManager.new())).to.be.ok()
        end)
    end)
    describe("connectEvent", function() -- Line: 23 -- upvalues: SingleEventManager (val), createSpy (val), assertDeepEqual (val), Logging (val)
        it("should connect to events", function() -- Line: 24 -- upvalues: SingleEventManager (upval), createSpy (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local v1 = SingleEventManager.new(BindableEvent)
            local v2 = createSpy()
            v1:connectEvent("Event", v2.value)
            v1:resume()
            BindableEvent:Fire("foo")
            expect(v2.callCount).to.equal(1)
            v2:assertCalledWith(BindableEvent, "foo")
            BindableEvent:Fire("bar")
            expect(v2.callCount).to.equal(2)
            v2:assertCalledWith(BindableEvent, "bar")
            v1:connectEvent("Event", nil)
            BindableEvent:Fire("baz")
            expect(v2.callCount).to.equal(2)
        end)
        it("should drop events until resumed initially", function() -- Line: 46 -- upvalues: SingleEventManager (upval), createSpy (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local v1 = SingleEventManager.new(BindableEvent)
            local v2 = createSpy()
            v1:connectEvent("Event", v2.value)
            BindableEvent:Fire("foo")
            expect(v2.callCount).to.equal(0)
            v1:resume()
            BindableEvent:Fire("bar")
            expect(v2.callCount).to.equal(1)
            v2:assertCalledWith(BindableEvent, "bar")
        end)
        it("should invoke suspended events when resumed", function() -- Line: 63 -- upvalues: SingleEventManager (upval), createSpy (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local v1 = SingleEventManager.new(BindableEvent)
            local v2 = createSpy()
            v1:connectEvent("Event", v2.value)
            v1:resume()
            BindableEvent:Fire("foo")
            expect(v2.callCount).to.equal(1)
            v2:assertCalledWith(BindableEvent, "foo")
            v1:suspend()
            BindableEvent:Fire("bar")
            expect(v2.callCount).to.equal(1)
            v1:resume()
            expect(v2.callCount).to.equal(2)
            v2:assertCalledWith(BindableEvent, "bar")
        end)
        it("should invoke events triggered during resumption in the correct order", function() -- Line: 85 -- upvalues: SingleEventManager (upval), createSpy (upval), assertDeepEqual (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local v1 = SingleEventManager.new(BindableEvent)
            local u7 = {}
            local v2 = createSpy(function(p1, p2) -- Line: 90 -- upvalues: u7 (val), BindableEvent (val)
                table.insert(u7, p2)
                if p2 == 2 then
                    BindableEvent:Fire(3)
                    return
                end
                if p2 == 3 then
                    BindableEvent:Fire(4)
                end
            end)
            v1:connectEvent("Event", v2.value)
            v1:suspend()
            BindableEvent:Fire(1)
            BindableEvent:Fire(2)
            v1:resume()
            expect(v2.callCount).to.equal(4)
            assertDeepEqual(u7, {1, 2, 3, 4})
        end)
        it("should not invoke events fired during suspension but disconnected before resumption", function() -- Line: 111 -- upvalues: SingleEventManager (upval), createSpy (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local v1 = SingleEventManager.new(BindableEvent)
            local v2 = createSpy()
            v1:connectEvent("Event", v2.value)
            v1:suspend()
            BindableEvent:Fire(1)
            v1:connectEvent("Event", nil)
            v1:resume()
            expect(v2.callCount).to.equal(0)
        end)
        it("should not yield events through the SingleEventManager when resuming", function() -- Line: 127 -- upvalues: SingleEventManager (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local u6 = SingleEventManager.new(BindableEvent)
            u6:connectEvent("Event", function() -- Line: 131
                coroutine.yield()
            end)
            u6:resume()
            local v1 = coroutine.create(function() -- Line: 137 -- upvalues: BindableEvent (val)
                BindableEvent:Fire(5)
            end)
            assert(coroutine.resume(v1))
            expect(coroutine.status(v1)).to.equal("dead")
            u6:suspend()
            BindableEvent:Fire(5)
            v1 = coroutine.create(function() -- Line: 147 -- upvalues: u6 (val)
                u6:resume()
            end)
            assert(coroutine.resume(v1))
            expect(coroutine.status(v1)).to.equal("dead")
        end)
        it("should not throw errors through SingleEventManager when resuming", function() -- Line: 155 -- upvalues: SingleEventManager (upval), Logging (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local u6 = SingleEventManager.new(BindableEvent)
            u6:connectEvent("Event", function() -- Line: 161
                error("Error from SingleEventManager test")
            end)
            u6:resume()
            u6:suspend()
            BindableEvent:Fire(5)
            local v1 = Logging.capture(function() -- Line: 174 -- upvalues: u6 (val)
                u6:resume()
            end)
            expect(#v1.errors).to.equal(0)
            expect(#v1.warnings).to.equal(1)
            expect(#v1.infos).to.equal(0)
            expect(v1.warnings[1]:find("Error from SingleEventManager test")).to.be.ok()
        end)
        it("should not overflow with events if manager:resume() is invoked when resuming a suspended event", function() -- Line: 185 -- upvalues: SingleEventManager (upval), createSpy (upval)
            local BindableEvent = Instance.new("BindableEvent")
            local u6 = SingleEventManager.new(BindableEvent)
            local v1 = createSpy(function(p1, p2) -- Line: 193 -- upvalues: u6 (val), BindableEvent (val)
                if p2 == 1 then
                    u6:suspend()
                    BindableEvent:Fire(2)
                    u6:resume()
                end
            end)
            u6:connectEvent("Event", v1.value)
            u6:suspend()
            BindableEvent:Fire(1)
            u6:resume()
            expect(v1.callCount).to.equal(2)
        end)
    end)
    describe("connectPropertyChange", function() -- Line: 211 -- upvalues: SingleEventManager (val), createSpy (val)
        it("should connect to property changes", function() -- Line: 215 -- upvalues: SingleEventManager (upval), createSpy (upval)
            local Folder = Instance.new("Folder")
            local v1 = SingleEventManager.new(Folder)
            local v2 = createSpy()
            v1:connectPropertyChange("Name", v2.value)
            v1:resume()
            Folder.Name = "foo"
            expect(v2.callCount).to.equal(1)
            v2:assertCalledWith(Folder)
            Folder.Name = "bar"
            expect(v2.callCount).to.equal(2)
            v2:assertCalledWith(Folder)
            v1:connectPropertyChange("Name")
            Folder.Name = "baz"
            expect(v2.callCount).to.equal(2)
        end)
        it("should throw an error if the property is invalid", function() -- Line: 237 -- upvalues: SingleEventManager (upval)
            local Folder = Instance.new("Folder")
            local u6 = SingleEventManager.new(Folder)
            expect(function() -- Line: 241 -- upvalues: u6 (val)
                u6:connectPropertyChange("foo", function() end)
            end).to.throw()
        end)
    end)
end